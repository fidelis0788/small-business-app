import LocationOnIcon from '@material-ui/icons/LocationOn';
import {  red } from '@material-ui/core/colors';

const LocationPin = () => (
  <div>
    <LocationOnIcon style={{color: red[500]}}/>
  </div>
)

export default LocationPin;
